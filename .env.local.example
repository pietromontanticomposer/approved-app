### .env.local.example
# Copy this file to .env.local and replace PLACEHOLDER with your actual
# Supabase Pooler (Supavisor) connection string in Transaction mode.
#
# How to get the Pooler connection string:
# - Supabase Dashboard → Database → Connection string
# - Method: Session pooler (Supavisor) / Pooler
# - Choose Transaction mode (uses port 6543)
# - Copy the URI shown (it looks like: postgresql://postgres:PASSWORD@...:6543/postgres)
#
# IMPORTANT:
# - Do NOT paste the URI into public places or chat.
# - For client-side usage, keep using NEXT_PUBLIC_SUPABASE_URL and
#   NEXT_PUBLIC_SUPABASE_ANON_KEY (do not replace them with the Pooler URI).
# - This value is intended for server-side code that uses `postgres` or
#   other server-side DB clients (serverless functions, migrations, etc.).

SUPABASE_DB_URL="postgresql://postgres:PLACEHOLDER@your-pooler-host.supabase.co:6543/postgres"

# If your deployment uses `DATABASE_URL` instead, set it too (choose one):
# DATABASE_URL="postgresql://postgres:PLACEHOLDER@your-pooler-host.supabase.co:6543/postgres"
